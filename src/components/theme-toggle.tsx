// Darkmode is intentionally disabled in this build — the toggle has been
// removed from the UI but this module is kept as a no-op so any stray
// import does not break the build.
export function ThemeToggle() {
  return null;
}
